<%= form_for(@foods_review) do |f| %>
  <% if @foods_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@foods_review.errors.count, "error") %> prohibited this foods_review from being saved:</h2>

      <ul>
      <% @foods_review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :food_id %><br>
    <%= f.number_field :food_id %>
  </div>
  <div class="field">
    <%= f.label :review_id %><br>
    <%= f.number_field :review_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
